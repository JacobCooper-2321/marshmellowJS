import { urlParse } from 'https://deno.land/x/url_parse/mod.ts';
const decoder = new TextDecoder();
export default function request(req: any, port: number) {
    const url = new URL

    keys.forEach((key: any) => (req[key] = parsedUrl[key]));
}
